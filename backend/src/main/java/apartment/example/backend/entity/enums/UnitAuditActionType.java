package apartment.example.backend.entity.enums;

/**
 * Enum for unit audit log action types
 */
public enum UnitAuditActionType {
    CREATED,
    UPDATED,
    PRICE_CHANGED,
    STATUS_CHANGED,
    DELETED,
    RESTORED
}
