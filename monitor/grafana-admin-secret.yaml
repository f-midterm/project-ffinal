apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin-secret
  namespace: superproject-ns
  labels:
    app: grafana
    component: monitoring
type: Opaque
stringData:
  # Default admin username
  admin-user: admin
  # Default admin password (change this in production!)
  # You can generate a secure password with: openssl rand -base64 32
  admin-password: "SuperSecure2024!"
