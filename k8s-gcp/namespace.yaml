# ============================================
# NAMESPACE
# ============================================
# Same namespace as localhost deployment for consistency
apiVersion: v1
kind: Namespace
metadata:
  name: superproject-ns
  labels:
    app: apartment-system
    environment: production
    platform: gcp
