# ============================================
# GKE MANAGED CERTIFICATE
# ============================================
# Google-managed SSL certificate for GKE Ingress
apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: apartment-certificate
  namespace: beliv-apartment
spec:
  domains:
    - beliv.pipatpongpri.dev
